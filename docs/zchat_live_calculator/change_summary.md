# Change Summary

Package adds a complete static calculator website under `docs/zchat_live_calculator/`.

## Added

- `index.html` with semantic calculator markup and local stylesheet/script references.
- `styles.css` with responsive layout, gradient background, glass-style panel, transitions, and animations.
- `app.js` with vanilla JavaScript arithmetic parsing, click handling, keyboard input, and display updates.
- `README.md` with usage and review notes.
- `context_readback.md` with source read report and fact separation.
- `verification/check_result.py` as an optional safe text-review helper.

## Scope

All deliverable files are under `docs/zchat_live_calculator/`.
